[
 {
  "ref": "FA-0792",
  "source": "fix-attack:new_bug",
  "severity": "low",
  "category": "new_bug",
  "detail": "The mechanism is real and verified against the code. create_connections (services.py:103-106) reconciles by deleting BOTH directions of 'similar' edges relative to the reconciling lesson's own current top-5 peer set. Its inbound clause Q(to_lesson=lesson) & ~Q(from_lesson_id__in=current_peer_ids) deletes Y->X edges (and paired X->Y) whenever Y is not in X's current top-5. Because the fix always writes both directions per peer (services.py:113-122) and find_similar_lessons caps at top-5 (limit=5, services.py:53), and cosine top-5 MEMBERSHIP is asymmetric even though similarity VALUE is symmetric (X may have 5 peers closer than Y, evicting Y from X's top-5 while X stays in Y's top-5), X's reconcile can authoritatively delete an edge that only Y's assessment created. The pre-fix accretion-only code (get_or_create only) never deleted these, so this IS a genuine NEW over-deletion the parent defect (which only asked to remove THIS lesson's stale OUTBOUND edges) did not request.\n\nThat said, the user/operator impact is effectively nil and self-healing, and I confirmed every bound the reviewer cites: (1) the dropped pair re-surfaces in BOTH constellation (views.py:148-179) and galaxy (views.py:226-256) as a recomputed 'affinity' edge, because any pair that was a stored 'similar' edge had similarity >= 0.75 (find_similar_lessons threshold, services.py:52), which always clears the affinity recompute floor (min_sim=0.3, or 0.1 for <=5 lessons) — so no edge vanishes from the rendered graph; (2) star-stage promotion via connections_out is monotonic/promote-only (growth.py:47-54), so a reduced count cannot demote; (3) the whole path is reachable only via the operator-run one-time rewrite_lessons_actionable command (plus reseed/backfill), not any user-facing edit endpoint — the same exposure ceiling the parent defect established. Net visible effect: a transient stored-'similar' -> recomputed-'affinity' edge-type drift (possibly a slightly different edge color/weight) for one pair until peer Y next reprocesses. No data loss, no demotion, no missing link. Genuine but purely cosmetic — same severity class as the parent defect, just a new behavioral wart rather than the one the fix targeted.",
  "suggested_fix": "Scope the inbound DELETE clause so the reconciling lesson is only authoritative over the edges IT authored, not over edges other lessons authored toward it. Two clean options: (a) Drop the inbound clause entirely and delete only outbound stale edges: filter(connection_type='similar', from_lesson=lesson).exclude(to_lesson_id__in=current_peer_ids).delete(), and rely on each peer's own reconcile to prune its outbound edges. This means a stale X->Y survives until X reprocesses, but it eliminates the cross-lesson over-deletion and keeps the symmetric-pair invariant looser. (b) Keep both-direction pruning but only delete a Y->X inbound edge when X is also absent from a recomputed check against Y (i.e. confirm the pair is non-similar from both sides before removing) — heavier, requires a per-edge similarity recheck. Given the impact is cosmetic and self-healing, option (a) is the proportionate fix; preserve the existing test_services idempotency assertion (created_again==0 when the similar set is unchanged) and keep the connection_type='similar'-only scoping so user-curated edges stay intact.",
  "fix_complexity": "small",
  "evidence": [
   "apps/lessons/services.py:103-106 — DELETE includes inbound clause Q(to_lesson=lesson) & ~Q(from_lesson_id__in=current_peer_ids), removing edges other lessons authored toward this one",
   "apps/lessons/services.py:113-122 — create_connections writes both from->peer and peer->lesson, so Y->X inbound edges exist whenever Y reconciled with X in its top-5",
   "apps/lessons/services.py:50-77 — find_similar_lessons caps at limit=5; top-5 membership is asymmetric across source lessons while cosine value is symmetric",
   "apps/lessons/views.py:148-179 and 226-256 — constellation + galaxy recompute affinity edges (min_sim 0.3/0.1) for any current-similar pair not stored; sim>=0.75 'similar' pairs always re-surface as 'affinity', masking the loss visually",
   "apps/lessons/growth.py:47-54 — connections_out feeds promote-only compute/apply_star_growth, so reduced count cannot cause a demotion",
   "apps/lessons/tests_audit_C02.py:69-91 — regression test asserts both-direction deletion of THIS lesson's stale peer; does not exercise the asymmetric cross-lesson (Y still-similar) case",
   "docs/feature-audit/DEFECTS.json FA-0792 — original defect asked only to remove THIS lesson's stale edges before recreate; both-direction over-reach is beyond scope"
  ]
 }
]