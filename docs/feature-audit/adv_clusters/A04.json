[
 {
  "ref": "FA-0533",
  "source": "fix-attack:incomplete",
  "severity": "medium",
  "category": "incomplete",
  "detail": "GENUINE incomplete fix. The fix for FA-0533 closed only the three named 'accounts' cases (order_by=is_debt, order_by=payoff_progress, group_by=is_debt) by carving them out of the order-by/group-by allowlists, with 7 accounts-only tests in tests_audit_C30.py. It left an identical FieldError->HTTP 500 hole on the 'transactions' resource in the very same _order() method.\n\nReproduction path (verified against the code):\n- query_views.py:332 — `_order` resolves the order-by allowlist as `_ALLOWED_ORDER_BY.get(resource, _ALLOWED_FIELDS[resource])`. `_ALLOWED_ORDER_BY` only has an 'accounts' key (line 128-130), so 'transactions' falls back to `_ALLOWED_FIELDS['transactions']`.\n- query_views.py:85-94 — `_ALLOWED_FIELDS['transactions']` includes `account_nickname`.\n- account_nickname is NOT a column on FinanceTransaction. models.py:94-119 shows the model's only fields are id, tenant, account (FK), transaction_type, amount, description, date, created_at. The serializer derives account_nickname from `t.account.nickname` (query_views.py:417) — a relation traversal, not a field.\n- query_views.py:333 — `account_nickname` is in the (fallback) allowlist, so the membership check passes.\n- query_views.py:338 — reaches `qs.order_by('account_nickname')`, where Django raises django.core.exceptions.FieldError (the join field would be `account__nickname`, not `account_nickname`).\n- apps/common/query_view.py:148-155 — `BaseQueryView.post` catches ONLY QueryExecutionError. No custom DRF EXCEPTION_HANDLER is configured (confirmed: zero matches in config/), so DRF's default handler does not convert FieldError, and it propagates to a Django 500.\n\nSo POST resource=transactions, order_by=account_nickname returns HTTP 500 — exactly the strict-400-contract violation FA-0533 was about. This is a natural agent query (transactions sorted by account name). The original defect explicitly named this invariant (\"order-by allowlist must reference real DB columns\") and explicitly recommended a belt-and-suspenders defense: wrap ORM execution to translate FieldError into QueryExecutionError(400). That defense was NOT applied, so the whole FieldError->500 class remains reachable; the fix papered over the two specifically-named accounts members instead of fixing the class. User impact is bounded: agent-only internal read surface (nbhd_gravity_query), no data corruption, just a 500 instead of a clean 400.",
  "suggested_fix": "Two-part, matching the original defect's own suggested_fix: (1) Add a `transactions` (and `plan`) entry to `_ALLOWED_ORDER_BY` listing only real DB columns — for transactions that is {id, account_id, transaction_type, amount, description, date, created_at} (drop account_nickname), so order_by=account_nickname returns the existing clean invalid_order_by 400; keep account_nickname in `_ALLOWED_FIELDS`/group_by where it is special-cased to account__nickname. (2) Belt-and-suspenders: in BaseQueryView.post (or _order/_aggregate) catch django.core.exceptions.FieldError and re-raise as QueryExecutionError(\"invalid_order_by\"/\"invalid_query\", status 400) so any future serializer-only field can never produce a 500. Add a test: POST resource=transactions, window={kind:all}, order_by=account_nickname -> 400.",
  "fix_complexity": "trivial",
  "evidence": [
   "query_views.py:332 — allowed = _ALLOWED_ORDER_BY.get(resource, _ALLOWED_FIELDS[resource]); _ALLOWED_ORDER_BY (line 128-130) only maps 'accounts', so transactions falls back to _ALLOWED_FIELDS['transactions']",
   "query_views.py:88 — 'account_nickname' is a member of _ALLOWED_FIELDS['transactions']",
   "models.py:94-119 — FinanceTransaction has no account_nickname column (fields: id, tenant, account FK, transaction_type, amount, description, date, created_at)",
   "query_views.py:417 — account_nickname is serialized from t.account.nickname (relation traversal, not a column)",
   "query_views.py:333-338 — membership check passes, then qs.order_by('account_nickname') raises FieldError",
   "apps/common/query_view.py:151 — post catches only QueryExecutionError; no EXCEPTION_HANDLER in config/ (grep: 0 matches) -> FieldError -> HTTP 500",
   "tests_audit_C30.py — all 7 new tests use resource:'accounts'; none exercise transactions order_by",
   "DEFECTS.json FA-0533.suggested_fix explicitly recommended wrapping FieldError->QueryExecutionError(400) as belt-and-suspenders; not applied"
  ]
 }
]