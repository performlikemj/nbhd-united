[
 {
  "ref": "router-chat#1",
  "source": "missed",
  "severity": "medium",
  "category": "ux",
  "detail": "Confirmed end-to-end. Each inbound iOS message creates one PENDING AppChatMessage (keyed by client_msg_id) AND one PendingMessage row, all with channel=ios and channel_user_id=thread.id (chat_views.py:163-243). When a user fires several messages to the same thread before the first reply lands (the common cold-start burst), _claim_pending_batch_for_key folds the contiguous PENDING rows into ONE coalesced batch — iOS rows are NOT excluded; only voice is forced singleton (pending_queue.py:518-539). _drain_ios_batch sends one combined OpenClaw turn via format_coalesced_user_content and gets ONE reply (pending_queue.py:1403-1432). _store_ios_turn_reply then writes that single text onto EVERY client_msg_id in the batch via .filter(...client_msg_id__in=client_ids).update(reply_text=text, status=READY,...) (pending_queue.py:1466-1476). The feed expands each AppChatMessage to its own assistant row app:<m.id>:1 with text=m.reply_text (chat_history.py:151-167) and dedups only by the synthetic id, never by content; build_since_page iterates per row (chat_history.py:260-264). ChatThreadMessagesView history has the same shape (chat_views.py:294-301 -> _serialize_message at 142). Result: 3 quick messages -> 3 user bubbles followed by the SAME assistant reply repeated 3x. Telegram/LINE escape this because their single coalesced reply is relayed ONCE to the channel API and never fanned out per persisted row. I also verified the secondary impact the reviewer claimed: the USER.md 'Conversation so far' digest reads AppChatMessage directly and appends {user, reply} per row (conversation_capture.py:247-257), so the same assistant text is duplicated N times in the LLM-facing digest. No test exercises iOS coalescing — test_ios_chat.py has no multi-message-into-one-batch case (the coalescing tests in test_pending_queue.py only cover Telegram/LINE relay-once, which is exactly why this per-row fan-out was never caught).</final_detail>\n<parameter name=\"suggested_fix\">In _store_ios_turn_reply, attach the combined reply to only ONE representative row of the coalesced batch (e.g. the last client_msg_id) while still flipping the other rows OFF pending so the client stops polling — but not emitting them as assistant rows. Concretely: set reply_text/READY on the representative row, and mark the other rows with a 'merged/answered' status (or set reply_text='' and a status that _app_rows / _serialize_message / the digest treat as 'no assistant row'). _app_rows already only emits the assistant row when status==READY and reply_text is non-empty, so leaving the non-representative rows with empty reply_text and a non-READY-but-terminal status would suppress the duplicates in all three surfaces (since-feed, thread history, and USER.md digest) at once. Singleton iOS batches (len==1) are unaffected and need no change. Add a test in test_ios_chat.py that enqueues 2-3 iOS messages into one thread, drains a single coalesced batch, and asserts the since-feed/history contain exactly one assistant row.",
  "suggested_fix": "",
  "fix_complexity": "small",
  "evidence": [
   "pending_queue.py:518-539 — _claim_pending_batch_for_key coalesces contiguous PENDING iOS rows; only voice forced singleton (no iOS exclusion)",
   "pending_queue.py:1466-1476 — _store_ios_turn_reply .update(client_msg_id__in=client_ids) writes the single combined reply onto every row in the batch",
   "pending_queue.py:1431-1432 — one ai_text for the whole batch",
   "chat_history.py:151-167 + 260-264 — _app_rows emits a distinct assistant row per AppChatMessage with that shared reply_text; build_since_page iterates per row; dedup is by synthetic id only",
   "chat_views.py:294-301, 142-160 — ChatThreadMessagesView/_serialize_message emits per-row, same duplication in thread history",
   "conversation_capture.py:247-257 — USER.md digest reads AppChatMessage per row and appends reply_text, duplicating the assistant text N times",
   "test_ios_chat.py — no multi-message coalescing test (function list shows none); Telegram/LINE relay-once tests don't cover the iOS per-row persistence fan-out"
  ]
 }
]