[
 {
  "ref": "pii#2",
  "source": "missed",
  "severity": "medium",
  "category": "data-integrity",
  "detail": "Confirmed at the code level. Every writer of pii_entity_map does an unsynchronized full-dict read-modify-write with no row lock, no F() expression, and no DB-side JSON merge: redactor (redactor.py:412-414, merged = {**existing_map, **new_map_entries} then update(pii_entity_map=merged)), memory_sync (memory_sync.py:76-83), arbiter (arbiter.py:277-285), and the settings views (views.py:1145 edit, 1172 delete, plus denylist writes 1249/1286). Grep confirms no select_for_update / transaction.atomic / F() / JSONB-merge on any of these writes, and there is no upstream per-tenant lock either: the redactor is invoked from three independent entry points that run as separate processes/requests — poller.py:1374 (Telegram drain), line_webhook.py:1248 (LINE), chat_views.py:226 (iOS chat) — plus the hourly arbiter cron and memory_sync.\n\nTwo real failure modes, both verified:\n(1) Placeholder-number collision / cross-entity name leak. The per-type counter is derived solely from the snapshot read at function start (redactor.py:323-328 scans existing_map for max N), then count = type_counters.get(etype,0)+1 (line 384). Two concurrent redactions that both read PERSON max=3 each mint [PERSON_4] for DIFFERENT people; the second update() (line 414) wins via {**existing_map, **new} and overwrites the first's [PERSON_4]. Message 1 was already sent to the LLM with [PERSON_4]=person A, but outbound rehydration re-reads the now-live map (pending_queue.py:1516-1521 and 1563-1568 call rehydrate_text on tenant.pii_entity_map) and substitutes person B's real name into the reply about person A — a genuine privacy leak.\n(2) Lost write / resurrected delete. A user DELETE (views.py:1171) or an arbiter denylist stamp that lands between a redactor's read and its full-dict write is silently clobbered — the deleted entry resurfaces, or the arbiter judged_at stamp is dropped.\n\nThis is correctly distinct from the known FA-0929 case-insensitive 'bloat' merge defects, which over-mint duplicates of the same entity; this finding is about lost writes and cross-entity name substitution from unsynchronized concurrent updates.\n\nSeverity is medium, not high. The race is real and the worst case (one contact's real name shown in another's context) is a true privacy leak, but the window is narrow (read-to-write within a single redaction) and the current production fleet is effectively one active, single-user tenant (canary). A single user rarely drives two truly concurrent inbound messages across channels simultaneously, though the hourly arbiter cron and memory_sync running against a live inbound message do widen the window. It is a latent correctness/privacy bug that will bite as the fleet grows or under multi-channel bursts, not a routinely-hit defect today.",
  "suggested_fix": "Serialize and atomicize each pii_entity_map mutation per tenant. Minimal: wrap each read-modify-write in transaction.atomic() with Tenant.objects.select_for_update().get(pk=...) to re-read the row under a lock before computing counters and merging, so the counter is derived from a locked snapshot and the merge cannot clobber a concurrent write. Best: move the merge into the DB with a JSONB concat (F('pii_entity_map') with a Postgres '||' merge / RawSQL jsonb_set) so new entries are added without overwriting keys written since the read; combined with select_for_update for the counter derivation, this closes both the collision and the lost-delete paths. Apply the same select_for_update pattern to the four settings views, the arbiter, and memory_sync.",
  "fix_complexity": "moderate",
  "evidence": [
   "apps/pii/redactor.py:323-328 type_counters derived from stale existing_map snapshot read at function start",
   "apps/pii/redactor.py:384-388 count = type_counters.get(etype,0)+1; placeholder = f'[{etype}_{count}]' — collides under concurrent reads of same max",
   "apps/pii/redactor.py:412-414 merged = {**existing_map, **new_map_entries}; objects.filter(pk=...).update(pii_entity_map=merged) — full overwrite, no lock",
   "apps/orchestrator/memory_sync.py:76-83 re-read then {**existing_map, **session.entity_map} full overwrite, no lock",
   "apps/pii/arbiter.py:277-285 updates['pii_entity_map']=entity_map; objects.filter(pk=...).update(**updates) — full overwrite, no lock",
   "apps/tenants/views.py:1171-1172 del entity_map[placeholder]; objects.filter(pk=...).update(pii_entity_map=entity_map) — delete via full overwrite, no lock",
   "apps/router/pending_queue.py:1516-1521 outbound rehydrate_text reads live tenant.pii_entity_map — picks up a concurrently-corrupted PERSON_N mapping",
   "grep across apps/pii and memory_sync: zero select_for_update/atomic/F()/JSONB-merge; redactor called from poller.py:1374, line_webhook.py:1248, chat_views.py:226 as independent processes"
  ]
 }
]