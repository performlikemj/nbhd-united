# Core
Django>=6.0.5,<6.1
djangorestframework>=3.15
django-cors-headers
django-environ
psycopg[binary]
gunicorn

# Auth
djangorestframework-simplejwt
PyJWT

# Task scheduling (QStash — replaces Celery)
qstash>=2.0

# Payments
dj-stripe>=2.9
stripe

# Telegram
python-telegram-bot>=21.0
qrcode[pil]

# AI / LLM
litellm>=1.86.0,<1.86.1  # 1.82.8 compromised — supply chain attack
openai
tiktoken
pgvector

# PII redaction (outgoing LLM traffic)
presidio-analyzer>=2.2    # Pattern recognizers only (credit card Luhn, IBAN checksum)
onnxruntime>=1.25.0
# Neural PII restoration: optimum 2.x splits onnx support into the
# `optimum-onnx` package and drops the broken `transformers.utils.is_tf_available`
# import that plagued 1.17. The tradeoff: optimum-onnx 0.1.0 caps
# transformers at <4.58, so we move OFF the 5.x range that prod has
# been stuck on (and ImportError-failing on every cold start since
# 2026-05-07). Onnbekend/nbhd-pii-model loads + infers cleanly on this
# pair locally; the existing apps/pii/tests.py integration tests pass.
# See issue #695.
transformers>=4.50.0,<4.58.0
optimum[onnxruntime]>=2.0,<3.0
sentencepiece>=0.2

# HTTP client (for routing to OpenClaw instances)
httpx

# Redis cache (shared across workers/containers)
django-redis

# Managed OAuth (Composio — Google integrations)
composio>=0.13.1

# Azure SDK (for Container Apps, Key Vault, Managed Identity, Storage, Billing)
azure-identity
azure-mgmt-containerinstance
azure-mgmt-appcontainers
azure-mgmt-costmanagement>=4.0
azure-mgmt-msi
azure-mgmt-authorization
azure-mgmt-storage
azure-storage-file-share
azure-keyvault-secrets

# Charts (server-side rendering for chat channels)
matplotlib

# Cron expression parsing (compute next fire time for hibernation wake)
croniter

# Utils
whitenoise
django-extensions
pip-tools
python-dateutil
