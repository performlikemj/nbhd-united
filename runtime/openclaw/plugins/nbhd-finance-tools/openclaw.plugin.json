{
  "id": "nbhd-finance-tools",
  "name": "NBHD Finance Tools",
  "description": "Budget tracking and debt payoff tools. Manage accounts, record payments, calculate payoff strategies (snowball, avalanche, hybrid), and track financial progress.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "description": "Override NBHD API base URL. Defaults to NBHD_API_BASE_URL env var."
      },
      "requestTimeoutMs": {
        "type": "number",
        "minimum": 1000,
        "maximum": 60000,
        "description": "HTTP timeout for NBHD runtime endpoint requests."
      }
    }
  }
}
