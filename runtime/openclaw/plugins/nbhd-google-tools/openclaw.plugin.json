{
  "id": "nbhd-google-tools",
  "name": "NBHD Google Tools",
  "description": "Read-only Gmail and Google Calendar tools backed by NBHD internal runtime endpoints.",
  "contracts": {
    "tools": [
      "nbhd_gmail_list_messages",
      "nbhd_gmail_get_message_detail",
      "nbhd_calendar_list_events",
      "nbhd_calendar_get_freebusy"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiBaseUrl": {
        "type": "string",
        "description": "Override NBHD API base URL. Defaults to NBHD_API_BASE_URL env var."
      },
      "requestTimeoutMs": {
        "type": "number",
        "minimum": 1000,
        "maximum": 60000,
        "description": "HTTP timeout for NBHD runtime endpoint requests."
      }
    }
  }
}
