# Isolated CPU evaluation dependencies for scripts/eval_pii_detectors.py.
# Install into the scratch venv documented in that script; do not add these to
# the Django runtime requirements.

torch==2.13.0
transformers==5.9.0
datasets==5.0.1
huggingface-hub==1.8.0
psutil==7.2.2
numpy==2.5.2
safetensors==0.7.0
