# Training dependencies — install on your GPU machine (Colab, Azure ML, etc.)
# NOT added to the Django requirements.txt

torch>=2.12.0
transformers>=5.9.0
datasets>=4.8.5
accelerate>=1.13.0
seqeval>=1.2.2
optimum[onnxruntime]>=2.1.0
onnxruntime>=1.26.0
numpy<3
sentencepiece>=0.2
