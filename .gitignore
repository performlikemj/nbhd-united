# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Environment
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Next.js
frontend/out/
frontend/.next/

# OS
.DS_Store
Thumbs.db

# Coverage
htmlcov/
.coverage
.coverage.*

# MCP config (may contain project refs)
.mcp.json

CONTINUITY*.md
CONTINUITY_*.md
CONTINUITY.md

# PII model (ONNX weights — not committed, baked into Docker image)
pii-model/
pii-model-output/
pii-model-test/
pii-model-multilingual/

# Claude Code / agent harness local state (per-machine, never commit)
.agents/
.claude/scheduled_tasks.lock
.claude/skills/
.claude/worktrees/
.claude/settings.local.json

# Locally-installed skill dependencies (managed via skills-lock.json)
# These are symlinks into .agents/skills/ — no trailing slash so git matches the symlink as a file.
skills-lock.json
skills/supabase
skills/supabase-postgres-best-practices

# HyperFrames compositions scratch dir
compositions/

# Local ML model artifacts (downloaded weights, training output)
models/